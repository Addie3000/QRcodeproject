# QRcodeproject
webdev project

I used CSS flexbox to complete this QR code component project. 
First built the main structure using html and providing elements with class, 
then styled the text with the correct fonts style and size.
used CSS flexbox properties to position the elements centered.
Then added the neccessary background colors.
